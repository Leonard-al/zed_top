import 'package:flutter/material.dart';

class CustomThemeScreen extends StatelessWidget {
  const CustomThemeScreen({Key? key}) : super(key: key);

  static const String id = 'themes';

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Container()));
  }
}
